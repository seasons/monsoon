type Query {
  search(query: String!, options: SearchOptions): [SearchResult]!
}

input SearchOptions {
  includeTypes: [String]
}

enum SearchResultType {
  Product
  Brand
  PhysicalProduct
}

union SearchResultData =
    ProductSearchResultData
  | PhysicalProductSearchResultsData

type SearchResult {
  kindOf: SearchResultType!
  data: SearchResultData!
}

type ProductSearchResultData {
  id: ID!
  slug: String!
  name: String!
  brandName: String!
  image: String
  description: String
  variantsCount: Int!
  physicalProductsCount: Int!
}

type PhysicalProductSearchResultsData {
  id: ID!
  productName: String!
  seasonsUID: String!
  inventoryStatus: InventoryStatus
  barcode: String!
}
