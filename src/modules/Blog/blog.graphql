type Query {
  blogCollections: [BlogCollection!]! @cacheControl(maxAge: 1000)
  blogPosts(skip: Int, count: Int): [BlogPost!]! @cacheControl(maxAge: 1000)
  blogPost(id: String): BlogPost @cacheControl(maxAge: 1000)
}

type BlogCollection {
  id: ID!
  slug: String!
  url: String
  fields: Json
  posts: [BlogPost!]!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type BlogPost {
  id: ID!
  slug: String!
  name: String
  body: String
  summary: String
  thumbnailURL: String
  imageURL: String
  imageAlt: String
  url: String
  tags: [String]
  createdAt: DateTime!
  updatedAt: DateTime!
  publishedOn: DateTime!
  author: String
  category: String
}
