describe("Test Suite Name", () => {
  /**
   * Declare services needed in all test cases
   */
  beforeAll(async () => {
    /**
     * Instantiate services needed in all test cases
     */
  })

  describe("Test Case 1", () => {
    /**
     * Declare variables needed in test
     */

    beforeAll(async () => {
      /**
       * Create the appropriate test state
       */
    }, ONE_MIN)

    afterAll(async () => {
      /**
       * Delete no longer needed data
       */
    })

    it("satisfies condition 1", () => {
      /**
       * Expects
       */
    })

    it("satisfies condition 2", () => {
      /**
       * Expects
       */
    })
  })

  describe("Test Case 2", () => {
    beforeAll(async () => {}, ONE_MIN)

    afterAll(async () => {})

    it("satisfies condition 1", () => {})

    it("satisfies condition 2", () => {})
  })
})
